<li>
  <%= gravatar_for user, size: Settings.gravatar.size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t(".link_to_delete"), user, method: :delete, data: { confirm: t(".link_to_confirm") } %>
  <% end %>
</li>
