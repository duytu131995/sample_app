<%= link_to gravatar_for(current_user, size: Settings.gravatar.size_micropost), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("shared.user_info.my_view_profile"), current_user %></span>
<span><%= pluralize current_user.microposts.count, t("shared.user_info.count") %></span>
